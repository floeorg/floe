// todo as function body placeholder
let processPayment(amount: number) -> number = {
  todo
}

// unreachable in match arm
let describe(n: number) -> string = {
  match n {
    1 -> "one",
    2 -> "two",
    _ -> unreachable,
  }
}

// todo in let
let placeholder = todo

// unreachable as expression
let guard = unreachable
