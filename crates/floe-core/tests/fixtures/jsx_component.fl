import trusted { useState, JSX } from "react"

export let Counter() -> JSX.Element = {
    let (_count, setCount) = useState(0)

    <div>
        <h1>Count</h1>
        <button onClick={setCount}>Increment</button>
    </div>
}
