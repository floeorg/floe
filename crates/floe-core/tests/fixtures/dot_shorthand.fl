let names = todos |> Array.map(.text)
let active = todos |> Array.filter(.done == false)
let byName = users |> Array.sortBy(.name)
let found = items |> Array.filter(.id != id)
let big = items |> Array.filter(.size > 100)
