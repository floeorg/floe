let data = 42
let transform(x: number) -> string = { "transformed" }
let format(s: string) -> string = { s }
let wrap(prefix: string, value: string, suffix: string) -> string = { prefix }

let _result = data |> transform |> format

let value = "hello"
let _placed = value |> wrap("(", _, ")")
