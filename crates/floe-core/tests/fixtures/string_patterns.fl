// String pattern matching with captures
let route(url: string) -> string = {
    match url {
        "/users/{id}" -> id,
        "/users/{id}/posts/{postId}" -> id,
        "/about" -> "about",
        "" -> "home",
        _ -> "not-found",
    }
}
