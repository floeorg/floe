export let add(a: number, b: number) -> number = {
    a + b
}

let _greet(name: string, greeting: string = "Hello") -> string = {
    `${greeting}, ${name}!`
}

let _double(n: number) -> number = { n * 2 }
