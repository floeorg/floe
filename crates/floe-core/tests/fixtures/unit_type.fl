// Unit type as return type
let log(msg: string) -> () = {
  console.log(msg)
}

// Unit type in Result generic
let deleteUser(id: string) -> Result<(), string> = {
  Ok(())
}

// Callback with unit return
type ButtonProps = {
  onClick: () -> ()
}
