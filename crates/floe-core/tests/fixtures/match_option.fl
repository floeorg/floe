let value: Option<string> = Some("hello")

let _result = match value {
    Some(s) -> s,
    None -> "nothing",
}

let empty: Option<number> = None

let _default = match empty {
    Some(n) -> n,
    None -> 0,
}
