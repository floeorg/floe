trait Display {
  let display(self) -> string
}

type User = {
  name: string,
  age: number
}

// Missing required method `display`
impl Display for User {
  let toString(self) -> string = {
    "wrong method name"
  }
}
