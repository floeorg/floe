let add(a: number, b: number) -> number = { a + b }
let multiply(a: number, b: number) -> number = { a * b }
let map(arr: Array<number>, f: (number) -> number) -> Array<number> = { arr }

let _addTen = add(10, _)
let _items = [1, 2, 3] |> map(multiply(_, 2))
