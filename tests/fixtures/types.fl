type User {
    id: string,
    name: string,
    email: string,
}

type Status {
    | Active
    | Inactive { reason: string }
    | Suspended(string)
    | Error(number, string)
}

type UserId { string }
